# RentEasy Full-Stack v2.0
See backend/README.md for full setup instructions.

Quick start:
  cd backend && npm install && cp .env.example .env && npm run seed && npm run dev
  cd frontend && npm install && npm start
